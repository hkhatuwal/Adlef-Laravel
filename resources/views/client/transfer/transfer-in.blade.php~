@extends('client._layouts.app')
@section('content')
    <div class="min-h-screen  py-8 " id="transfer-in">
        <div class="container mx-auto px-4 sm:px-6 lg:px-8">
            <div class="max-w-3xl mx-auto">
                <!-- Header with Progress -->
                <div class="mb-6">
                    <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between mb-4">
                        <div>
                            <h1 class="text-2xl sm:text-3xl font-bold text-gray-900 mb-3 sm:mb-0">Create Transfer
                                In</h1>
                            <p class="text-sm text-gray-500">Transfer money from your Adlef Account to bank </p>
                        </div>
                        <div class="flex items-center space-x-2">
                            <span class="text-sm font-medium step-text text-gray-500"
                                  data-step="1">Account Details</span>
                            <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                            </svg>
                            <span class="text-sm font-medium step-text text-gray-500"
                                  data-step="2">Amount & Details</span>
                        </div>
                    </div>
                    <div class="h-2 bg-gray-200 rounded-full">
                        <div class="h-2 bg-black rounded-full transition-all duration-300 progress-bar"></div>
                    </div>
                </div>

                <form id="transferForm" action="{{ route('client.transfer.in.post') }}" method="POST" class="space-y-6">
                    @csrf
                    <input type="hidden" name="isUSD" id="isUSD" value="{{$assetAccount->currency->isUSD()}}">
                    <input type="hidden" name="currency_id" value="{{request()->query('currency_id')}}">

                    <!-- Account Details -->
                    <div class="space-y-4 step-content" id="step1">
                        <!-- From Account Section -->

                        @if($assetAccount->currency->isFiat())
                            <div class="bg-white rounded-lg shadow-sm border border-gray-100 p-4 sm:p-6">
                                <div class="flex items-center justify-between mb-4">
                                    <h2 class="text-lg sm:text-xl font-semibold text-gray-900">From Account</h2>
                                    <div id="balanceInfo" class="hidden text-sm text-gray-500">
                                        Available: <span id="availableBalance" class="font-medium"></span>
                                    </div>
                                </div>
                                <div class="space-y-4">
                                    <div class="relative">
                                        <select name="from_account" id="fromAccount" class="select2">
                                            <option value="">Select Your Bank Account</option>
                                            @foreach($fromAccounts as $account)
                                                <option value="{{ $account->id }}" @selected($loop->index==0)>
                                                    {{ $account->bank_name }} - {{ $account->account_number }}
                                                    ({{ $account->account_type }})
                                                </option>
                                            @endforeach
                                        </select>
                                        <div class="error-message hidden mt-2 text-sm text-red-600"></div>
                                    </div>

                                </div>
                            </div>

                        @endif

                        <!-- To Account Section (Recipient User) -->
                        <div class="bg-white rounded-lg shadow-sm border border-gray-100 p-4 sm:p-6">
                            <div class="flex items-center justify-between mb-4">
                                <h2 class="text-lg sm:text-xl font-semibold text-gray-900">To Account</h2>
                                <span class="px-3 py-1 text-xs font-medium bg-blue-50 text-blue-600 rounded-full">Recipient</span>
                            </div>
                            <div class="p-4 bg-gray-50 rounded-lg">
                                <div class="flex items-center justify-between">
                                    <div class="flex items-center">
                                        <img src="{{ asset('storage/' . $assetAccount->currency->icon) }}"
                                             alt="{{  $assetAccount->name }}"
                                             class="w-10 h-10 sm:w-12 sm:h-12 rounded-full object-contain">

                                        <div class="ml-4">
                                            <h3 class="font-medium text-gray-900">{{  $assetAccount->name }}</h3>
                                            <p class="text-sm text-gray-500">A/C
                                                : {{ $assetAccount->account_number }}</p>
                                        </div>
                                    </div>
                                    <div class="flex flex-col items-end">
                                        <span class="text-sm font-medium text-gray-700">Balance:</span>
                                        <span
                                            class="text-lg font-semibold text-green-600">${{ $assetAccount->balance }}</span>
                                    </div>
                                    <input type="hidden" name="to_account" value="{{ $assetAccount->id }}">
                                </div>
                            </div>
                        </div>

                        <!-- Source of Account Section -->
                        <div class="bg-white rounded-lg shadow-sm border border-gray-100 p-4 sm:p-6">
                            <div class="mb-4">
                                <h2 class="text-lg sm:text-xl font-semibold text-gray-900">Source of Account</h2>
                                <p class="mt-1 text-sm text-gray-500">Select the source of funds for this transfer</p>
                            </div>
                            <div class="relative">
                                <select name="source_of_account" id="sourceAccount" class="select2">
                                    <option value="">Select Source</option>
                                    @foreach($sourceOptions as $value => $label)
                                        <option value="{{ $value }}" @selected($loop->index==0)>{{ $label }}</option>
                                    @endforeach
                                </select>
                                <div class="error-message hidden mt-2 text-sm text-red-600"></div>
                            </div>
                        </div>
                    </div>

                    <!-- Amount & Notes Section -->
                    <div class="space-y-4 step-content hidden" id="step2">
                        <!-- Transfer Amount -->
                        <div class="bg-white rounded-lg shadow-sm border border-gray-100 p-4 sm:p-6">
                            <div class="flex items-center justify-between mb-4">
                                <h2 class="text-lg sm:text-xl font-semibold text-gray-900">Transfer Amount</h2>
                                <div class="text-sm text-gray-500">
                                    Available: <span id="available-balance" class="font-medium"></span>
                                </div>
                            </div>
                            <div class="space-y-4">
                                <div class="relative">
                                    <input type="number"
                                           name="amount"
                                           id="amount"
                                           step="0.01"
                                           min="0"
                                           class="!pl-9"
                                           placeholder="0.00">
                                    <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                                        <span class="text-xl sm:text-2xl font-semibold text-gray-500">$</span>
                                    </div>
                                    <div class="error-message hidden mt-2 text-sm text-red-600"></div>
                                </div>
                            </div>
                        </div>

                        <!-- Notes Section -->
                        <div class="bg-white rounded-lg shadow-sm border border-gray-100 p-4 sm:p-6">
                            <div class="mb-4">
                                <h2 class="text-lg sm:text-xl font-semibold text-gray-900">Additional Notes</h2>
                                <p class="mt-1 text-sm text-gray-500">Add any notes or references for this transfer
                                    (optional)</p>
                            </div>
                            <textarea name="notes"
                                      id="notes"
                                      rows="3"
                                      class="focus:ring-black block w-full bg-white p-4 rounded-lg border shadow-sm sm:text-lg border-gray-300 resize-none"
                                      placeholder="e.g., Monthly rent payment"></textarea>
                        </div>
                    </div>

                    <!-- Action Buttons -->
                    <div class="flex items-center justify-between pt-4">
                        <div id="backButton">
                            <a href="{{ route('client.transfer') }}"
                               class="inline-flex items-center px-4 sm:px-6 py-2 sm:py-3 border-2 border-gray-200 text-base font-medium rounded-full text-gray-700 bg-white hover:bg-gray-50 hover:border-gray-300 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-black transition-all">
                                <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                          d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
                                </svg>
                                Back
                            </a>
                        </div>
                        <div id="prevButton" class="hidden">
                            <button type="button"
                                    class="inline-flex items-center px-4 sm:px-6 py-2 sm:py-3 border-2 border-gray-200 text-base font-medium rounded-full text-gray-700 bg-white hover:bg-gray-50 hover:border-gray-300 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-black transition-all">
                                <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                          d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
                                </svg>
                                Previous
                            </button>
                        </div>

                        <button type="button" id="nextButton"
                                class="inline-flex items-center px-6 sm:px-8 py-2 sm:py-3 border-2 border-black text-base font-medium rounded-full text-white bg-black hover:bg-gray-800 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-black transition-all disabled:opacity-50 disabled:cursor-not-allowed">
                            Next
                            <svg class="ml-2 -mr-1 w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                      d="M14 5l7 7m0 0l-7 7m7-7H3"/>
                            </svg>
                        </button>

                        <button type="submit" id="submitButton"
                                class=" flex flex-row items-center px-6 sm:px-8 py-2 sm:py-3 border-2 border-black text-base font-medium rounded-full text-white bg-black hover:bg-gray-800 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-black transition-all disabled:opacity-50 disabled:cursor-not-allowed">
                            <span class="submit-text">Continue to Review </span>
                            <span class="loading-text hidden flex items-center">
                            <svg class="animate-spin -ml-1 mr-3 h-5 w-5 text-white" fill="none" viewBox="0 0 24 24">
                                <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor"
                                        stroke-width="4"></circle>
                                <path class="opacity-75" fill="currentColor"
                                      d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
                            </svg>
                            Processing...
                        </span>
                            <svg class="submit-icon ml-2 -mr-1 w-5 h-5" fill="none" stroke="currentColor"
                                 viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                      d="M14 5l7 7m0 0l-7 7m7-7H3"/>
                            </svg>
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>

@endsection


